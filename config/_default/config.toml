languageCode = "en-esp"
title = " Cocket Nova & Arduino IDE "
theme = "book"

# Needed for mermaid/katex shortcodes
[markup]
    [markup.goldmark.renderer]
        unsafe = true

    [markup.tableOfContents]
        startLevel = 1

BookSearch = true